<% provide(:purchase_url, params[:book][:purchase_url]) %>
<% provide(:image, params[:book][:image]) %>
<% provide(:title, params[:book][:title]) %>
<% provide(:price, params[:book][:price]) %>
<% provide(:proper_title, params[:book][:proper_title]) %>
<% provide(:author, params[:book][:author]) %>

<% provide(:youtube_links, 'books/youtube_links_new' ) %>
<% provide(:tags_checkbox, 'books/tags_checkbox' ) %>

<div id="book_new_page" class="container card">

<%= render 'books/books_form' %>

</div>